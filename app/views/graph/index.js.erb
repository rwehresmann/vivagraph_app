$('body').css('cursor', 'default');
$('#select-info').remove();

// The link only appears when the animation is played again.
if ($('#pause-resume i').attr('class') == 'fa fa-play') {
  resume();
  pause();
}
